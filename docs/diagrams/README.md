# Diagrams

Diagrams live **inline as Mermaid** in:

- `docs/PLAN.md`
- `docs/ARCHITECTURE.md`
- `docs/ROADMAP.md`
- `docs/DATA_MODEL.md`
- `docs/PROTOCOL.md`

Export PNGs here later (e.g. mermaid-cli) for decks or Jira if needed:

```bash
# example
npx @mermaid-js/mermaid-cli -i ../ARCHITECTURE.md -o architecture.png
```
