services:
  railspan:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "7421:7421"
    environment:
      RAILSPAN_INGEST_ADDR: "0.0.0.0:7421"
      RAILSPAN_API_KEY: ""
    # Placeholder image until release packaging; build may be used after Dockerfile is filled in.
