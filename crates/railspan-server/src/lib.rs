//! Query API and persistence — implemented in Phase E3.
//!
//! For now this crate is a placeholder so the workspace builds cleanly.

/// Placeholder until storage lands.
pub fn placeholder() -> &'static str {
    "railspan-server stub"
}

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder_works() {
        assert_eq!(super::placeholder(), "railspan-server stub");
    }
}
